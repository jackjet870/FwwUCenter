@model IEnumerable<WorkflowEngine.wfDefinition>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.wfCode)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.wfName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.wfCode)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.wfName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.wfCode }) |
            @Html.ActionLink("Details", "Details", new { id=item.wfCode }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.wfCode })
        </td>
    </tr>
}

</table>
